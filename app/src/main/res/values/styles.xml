<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="android:Theme.Holo.Light.DarkActionBar">
        <item name="android:actionBarTheme">@style/MyActionBar</item>
        <!-- Customize your theme here. -->
    </style>
    
    <style name="MyActionBar" parent="android:Widget.Holo.ActionBar.Solid" >
        <item name="android:background">@color/primary_color</item>
    </style>

    <style name="MyText" parent="android:TextAppearance" >
        <item name="android:textColor">@color/title_bar_text_color</item>

</style>

</resources>
